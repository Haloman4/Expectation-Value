import sympy as sp


def Expectation(p, x):
  E = x * p
  return E

p = sp.Matrix([1/10, 2/10, 3/10, 4/10])
x = sp.Matrix([1, 2, 3, 4])
x2 = x.T

display(Expectation(p, x2))

x_squared_row = x2.applyfunc(lambda element: element**2)

display(Expectation(p, x_squared_row)-Expectation(p, x2)**2)
